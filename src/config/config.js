module.exports = {
    // Pagination defaults
    pagination: {
      defaultLimit: 10,
      maxLimit: 100
    },
    
    // Tag value simulation settings
    simulation: {
      updateInterval: 5000, // 5 seconds
      enabled: true
    }
  };